# The version of Immich to use. We recommend starting with a specific tag instead of 'latest'.
IMMICH_VERSION=v1.94.1

# The URL for your Immich server. Replace with your server's IP or domain name.
IMMICH_SERVER_URL=http://<your-server-ip>:2283

# User and Group IDs for the container. This is a best practice for file permissions.
# Find them by running `id -u` and `id -g` in your terminal.
PUID=1000
PGID=1000

# The PostgreSQL database configuration.
# POSTGRES_DB: The name of the database to be created.
POSTGRES_DB=immich
# POSTGRES_USER: The username for the database.
POSTGRES_USER=immich
# POSTGRES_PASSWORD: The password for the database user.
# Remember to use a strong, unique password.
POSTGRES_PASSWORD=your_strong_password
